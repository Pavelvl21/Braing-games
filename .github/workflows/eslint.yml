#Check code in project with ESlint
name: Lint
on: push
jobs:
  eslint-check:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - run: npm install --save-dev eslint-config-airbnb-base
      - run: make lint
      